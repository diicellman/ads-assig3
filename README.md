# ADS Assignment 3

Plain Java project layout:

- `src/` contains the Java source files.
- `out/` is used for compiled `.class` files.
- `instruction.md` contains the original assignment requirements.

Compile:

```bash
javac -Xlint:unchecked -d out src/*.java
```

Run:

```bash
java -cp out Main
```
